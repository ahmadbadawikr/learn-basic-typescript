import { MathUtil } from "../src/math-util";
describe("Namespace", () => {
    it("Should support", () => {
        console.info(MathUtil.PI*2000)

        console.info(MathUtil.sum(1,2,3,4,5,6))
    })

})